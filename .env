# P2P Transfer Hub Configuration
# No MongoDB required - all data is in-memory!

# Temporary file storage for paired device transfers
UPLOADS_PATH=backend/uploads

# Pairing code expiration time (seconds)
PAIRING_CODE_TTL=3600

# Maximum concurrent pairings allowed
MAX_CONNECTIONS=100
